#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@remix-project/remix-simulator/bin/ethsim" "$@"
  ret=$?
else 
  node  "$basedir/../@remix-project/remix-simulator/bin/ethsim" "$@"
  ret=$?
fi
exit $ret
